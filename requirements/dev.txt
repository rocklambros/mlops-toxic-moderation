-r base.txt
pytest==9.0.3
ruff==0.7.4
# Integration tests run against a real Postgres, never SQLite: the schema uses JSONB and
# ON CONFLICT, and delivery spec section 3.3 makes a real dependency the phase gate.
testcontainers[postgres]==4.8.2
# `fastapi.testclient.TestClient` is an httpx client; without this the /predict and /health
# suites cannot even be collected. Test-only, so it stays out of requirements/serve.in and
# never reaches the production image.
httpx==0.27.2
# `tests/unit/test_severability.py` parses infra/docker-compose.yml as data. The alternative
# -- asserting against the file as text -- would pass on a compose file that does not parse,
# which is the one property `docker compose config` already checks. Test-only: no image
# installs it, and nothing in the application reads YAML.
PyYAML==6.0.2
